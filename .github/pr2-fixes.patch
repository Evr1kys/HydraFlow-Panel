diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 90b2f3d..ed9a369 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -57,6 +57,8 @@ jobs:
           node-version: '22'
           cache: 'npm'
           cache-dependency-path: backend/package-lock.json
+      - name: Pin npm version
+        run: npm install --global npm@10.9.2
       - name: Install dependencies
         working-directory: backend
         run: npm ci
@@ -113,6 +115,8 @@ jobs:
           node-version: '22'
           cache: 'npm'
           cache-dependency-path: frontend/package-lock.json
+      - name: Pin npm version
+        run: npm install --global npm@10.9.2
       - name: Install dependencies
         working-directory: frontend
         run: npm ci
diff --git a/backend/package.json b/backend/package.json
index 4803023..8cb8587 100644
--- a/backend/package.json
+++ b/backend/package.json
@@ -81,6 +81,7 @@
     "ts-node": "^10.9.2",
     "typescript": "^5.3.3"
   },
+  "packageManager": "npm@10.9.2",
   "overrides": {
     "axios": "1.20.0"
   }
diff --git a/backend/src/nodes/dto/nodes-paginated-query.dto.ts b/backend/src/nodes/dto/nodes-paginated-query.dto.ts
index 0ac5c12..549f6f5 100644
--- a/backend/src/nodes/dto/nodes-paginated-query.dto.ts
+++ b/backend/src/nodes/dto/nodes-paginated-query.dto.ts
@@ -12,7 +12,13 @@ function toBool(value: unknown): boolean | undefined {
   return undefined;
 }
 
-export const NODE_STATUS_VALUES = ['healthy', 'error', 'unknown'] as const;
+export const NODE_STATUS_VALUES = [
+  'healthy',
+  'degraded',
+  'offline',
+  'error',
+  'unknown',
+] as const;
 export type NodeStatusFilter = (typeof NODE_STATUS_VALUES)[number];
 
 export class NodesPaginatedQueryDto extends PaginationQueryDto {
diff --git a/backend/src/nodes/nodes.service.ts b/backend/src/nodes/nodes.service.ts
index 772a98f..11263b0 100644
--- a/backend/src/nodes/nodes.service.ts
+++ b/backend/src/nodes/nodes.service.ts
@@ -397,29 +397,21 @@ export class NodesService {
     initiatedBy?: string,
   ): Promise<SyncResult> {
     const idempotencyKey = `cfg:${node.id}:${hash}`;
-    const existing = await this.prisma.nodeDeployment.findUnique({
-      where: { idempotencyKey },
-    });
-    if (existing?.status === 'succeeded') {
+    const deployment = await this.getOrCreateDeployment(
+      node.id,
+      idempotencyKey,
+      hash,
+      initiatedBy,
+    );
+    if (deployment.status === 'succeeded') {
       return {
         nodeId: node.id,
         success: true,
-        revision: existing.revision ?? undefined,
-        deploymentId: existing.id,
+        revision: deployment.revision ?? undefined,
+        deploymentId: deployment.id,
       };
     }
 
-    const deployment = existing ??
-      (await this.prisma.nodeDeployment.create({
-        data: {
-          nodeId: node.id,
-          idempotencyKey,
-          configHash: hash,
-          initiatedBy,
-          status: 'pending',
-        },
-      }));
-
     try {
       const credentials = await this.credentials(node);
       const validation = await this.agentClient.validate(credentials, config);
@@ -498,6 +490,53 @@ export class NodesService {
     }
   }
 
+  private async getOrCreateDeployment(
+    nodeId: string,
+    idempotencyKey: string,
+    configHash: string,
+    initiatedBy?: string,
+  ) {
+    const existing = await this.prisma.nodeDeployment.findUnique({
+      where: { idempotencyKey },
+    });
+    if (existing) return existing;
+
+    try {
+      return await this.prisma.nodeDeployment.create({
+        data: {
+          nodeId,
+          idempotencyKey,
+          configHash,
+          initiatedBy,
+          status: 'pending',
+        },
+      });
+    } catch (error) {
+      // Parallel requests for the same desired config are expected to race on
+      // the unique idempotency key. Re-read the winner instead of reporting a
+      // false deployment failure.
+      if (this.isUniqueConstraintError(error)) {
+        const concurrent = await this.prisma.nodeDeployment.findUnique({
+          where: { idempotencyKey },
+        });
+        if (concurrent) return concurrent;
+      }
+      throw error;
+    }
+  }
+
+  private isUniqueConstraintError(error: unknown): boolean {
+    return (
+      error instanceof Prisma.PrismaClientKnownRequestError &&
+      error.code === 'P2002'
+    ) || (
+      typeof error === 'object' &&
+      error !== null &&
+      'code' in error &&
+      (error as { code?: unknown }).code === 'P2002'
+    );
+  }
+
   private parseConfig(config: string): unknown {
     try {
       const parsed = JSON.parse(config) as unknown;
diff --git a/backend/test/nodes.service.spec.ts b/backend/test/nodes.service.spec.ts
index 17ff99b..72b40e9 100644
--- a/backend/test/nodes.service.spec.ts
+++ b/backend/test/nodes.service.spec.ts
@@ -171,6 +171,43 @@ describe('NodesService Agent integration', () => {
     });
   });
 
+  it('reuses a concurrent deployment created by another request', async () => {
+    const node = makeNode();
+    const existing = {
+      id: 'deployment-concurrent',
+      nodeId: node.id,
+      idempotencyKey: 'cfg:node-1:hash',
+      configHash: 'hash',
+      revision: 'rev-concurrent',
+      status: 'succeeded',
+      error: null,
+      initiatedBy: 'admin-1',
+      createdAt: new Date(),
+      finishedAt: new Date(),
+    };
+    mockPrisma.node.findMany.mockResolvedValue([node]);
+    mockPrisma.nodeDeployment.findUnique
+      .mockResolvedValueOnce(null)
+      .mockResolvedValueOnce(existing);
+    mockPrisma.nodeDeployment.create.mockRejectedValue({ code: 'P2002' });
+
+    const results = await createService().pushConfigToAll(
+      JSON.stringify({ inbounds: [], outbounds: [] }),
+      'admin-1',
+    );
+
+    expect(results).toEqual([
+      {
+        nodeId: node.id,
+        success: true,
+        revision: 'rev-concurrent',
+        deploymentId: 'deployment-concurrent',
+      },
+    ]);
+    expect(mockAgent.validate).not.toHaveBeenCalled();
+    expect(mockAgent.apply).not.toHaveBeenCalled();
+  });
+
   it('reuses a successful deployment without calling the Agent', async () => {
     const node = makeNode();
     mockPrisma.node.findMany.mockResolvedValue([node]);
diff --git a/frontend/package.json b/frontend/package.json
index c9c2c2d..395162e 100644
--- a/frontend/package.json
+++ b/frontend/package.json
@@ -39,6 +39,7 @@
     "typescript": "^5.3.3",
     "vite": "5.4.21"
   },
+  "packageManager": "npm@10.9.2",
   "overrides": {
     "axios": "1.20.0"
   }
